# Nazwa profilu z `~/.aws/credentials` (utworzonego przez `aws configure --profile <nazwa>`).
# Nigdy nie wpisuj tu Access Key/Secret Key bezpośrednio — boto3/CLI czytają je z
# ~/.aws/credentials automatycznie, ta zmienna tylko wskazuje, którego profilu użyć.
AWS_PROFILE=amazon-books-etl-dev

# Sekrety
.env

# Python
.venv/
__pycache__/
*.pyc

# Dane pipeline'u (CSV ze scrapowania)
data/

# Logi
logs/
*.log

# Airflow — runtime, nie kod (DAG-i zostają w repo)
airflow/logs/
airflow/airflow.db
airflow/airflow.cfg
airflow/simple_auth_manager_passwords.json.generated

# dbt — artefakty kompilacji
dbt_project/target/
dbt_project/logs/
dbt_project/dbt_packages/

# dbt-duckdb — lokalny plik bazy (dev-loop), nie commitować
*.duckdb
*.duckdb.wal

# Claude Code
.claude/
CLAUDE.md

# docs — tylko architecture.md w repo
docs/*
!docs/architecture.md
!docs/RUNBOOK.md

# moje notatki
NOTEBOOK.md

# scrape.yml — tymczasowo przeniesiony ze .github/workflows/, nie commitować w tej formie
scripts/scrape.yml

# Lambda — artefakty pakowania (regenerowalne, nie źródło)
build/
lambda_scraper.zip